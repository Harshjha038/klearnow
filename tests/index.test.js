import './__mocks__/env-config'; // Mocks necessary window env vars.
import { uploadImageFileData } from '../src/actions/Container';

test('This should always pass', () => {
  expect(true).toBe(true);
})